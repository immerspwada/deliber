# 🔄 Clear Cache Instructions (ภาษาไทย)

**วันที่**: 2026-01-26  
**สถานะ**: 📋 คำแนะนำทีละขั้นตอน

---

## 🎯 ปัญหา

โค้ดใหม่ที่เราเพิ่มไปไม่ทำงาน เพราะ **Browser ใช้โค้ดเก่าที่ Cache ไว้**

---

## ✅ วิธีแก้ (5 ขั้นตอน)

### ขั้นตอนที่ 1: หยุด Dev Server

```bash
# กด Ctrl+C ใน Terminal
```

### ขั้นตอนที่ 2: ลบ Cache ของ Vite

```bash
rm -rf node_modules/.vite
```

### ขั้นตอนที่ 3: เริ่ม Dev Server ใหม่

```bash
npm run dev
```

รอจนเห็นข้อความ:

```
VITE v6.x.x  ready in XXX ms
➜  Local:   http://localhost:5173/
```

### ขั้นตอนที่ 4: ลบ Cache ของ Browser

#### วิธีที่ 1: Hard Reload (แนะนำ)

1. เปิด DevTools (กด F12)
2. **คลิกขวา** ที่ปุ่ม Refresh (ข้างๆ Address Bar)
3. เลือก **"Empty Cache and Hard Reload"**

#### วิธีที่ 2: Clear Storage (ถ้าวิธีที่ 1 ไม่ได้ผล)

1. เปิด DevTools (กด F12)
2. ไปที่แท็บ **Application**
3. คลิก **"Clear storage"** ทางซ้าย
4. เลือกทุกอย่าง:
   - ✅ Application cache
   - ✅ Cache storage
   - ✅ Service workers
   - ✅ Local storage
   - ✅ Session storage
   - ✅ IndexedDB
5. คลิก **"Clear site data"**

### ขั้นตอนที่ 5: ทดสอบใน Incognito Window

1. **ปิดทุก Tab** ที่เปิด `localhost:5173`
2. เปิด **Incognito/Private Window** ใหม่
   - Chrome: `Ctrl+Shift+N`
   - Firefox: `Ctrl+Shift+P`
3. ไปที่: `http://localhost:5173/customer/ride`
4. เปิด Console (กด F12)
5. ล้าง Console (กด `Ctrl+L`)

---

## 🧪 ทดสอบ

1. คลิกบนแผนที่เพื่อเลือก **จุดรับ** (ถ้ายังไม่มี)
2. คลิกบนแผนที่อีกครั้งเพื่อเลือก **จุดหมาย**
3. **ดูที่ Console**

---

## ✅ ผลลัพธ์ที่ควรเห็น

### Console ควรแสดง:

```
[MapView] 🗺️ Map clicked!
[RideView] Geocoded via photon: Charoen Khet Road

[useRideRequest.calculateFare] ===== CALLED =====  ← ต้องเห็นบรรทัดนี้!
[useRideRequest.calculateFare] Pickup: {...}
[useRideRequest.calculateFare] Destination: {...}
[useRideRequest.calculateFare] Distance calculated: 2.5 km

[calculateFare] Database base fare: {...}
[calculateFare] Final fare with multiplier: {...}
```

### ราคาควรเปลี่ยน:

**ก่อนลบ Cache (ราคาผิด):**

- 🚲 มอเตอร์ไซค์: ฿35
- 🚗 รถยนต์: ฿50
- 💎 พรีเมียม: ฿75

**หลังลบ Cache (ราคาถูกต้อง - ตัวอย่าง 2.5 กม.):**

- 🚲 มอเตอร์ไซค์: ฿42 (60 × 0.7)
- 🚗 รถยนต์: ฿60 (60 × 1.0)
- 💎 พรีเมียม: ฿90 (60 × 1.5)

_ราคาจะเปลี่ยนตามระยะทางจริง_

---

## 🐛 ถ้ายังไม่ได้

### ถ้ายังไม่เห็น Log ใน Console:

1. ตรวจสอบว่า Dev Server รันอยู่
2. ตรวจสอบว่าไม่มี Error สีแดงใน Terminal
3. ลองใช้ Browser อื่น (Firefox, Safari)
4. ลอง Nuclear Option:
   ```bash
   rm -rf node_modules
   npm install
   npm run dev
   ```

### ถ้าเห็น Log แต่ราคายังผิด:

1. ดูที่ Console ว่ามี Error อะไร
2. ตรวจสอบว่า `estimatedFare` อัพเดทหรือไม่
3. ตรวจสอบว่า Vehicle Multipliers โหลดหรือไม่

---

## 📸 ส่งผลลัพธ์

หลังจากลบ Cache แล้ว กรุณาส่ง:

1. **Screenshot ของ Console** (ทั้งหมด)
2. **Screenshot ของราคา** (3 ประเภทรถ)
3. **บอกว่าเห็น Log หรือไม่**

---

## 🎯 สรุป

**โค้ดพร้อมแล้ว** แค่ต้องลบ Cache ของ Browser เพื่อให้โหลดโค้ดใหม่

**ขั้นตอนสำคัญ:**

1. ✅ หยุด Dev Server
2. ✅ ลบ `node_modules/.vite`
3. ✅ เริ่ม Dev Server ใหม่
4. ✅ Hard Reload Browser
5. ✅ ทดสอบใน Incognito Window

---

**ถ้าทำตามแล้วยังไม่ได้ ให้ส่ง Screenshot Console มาดูครับ**
