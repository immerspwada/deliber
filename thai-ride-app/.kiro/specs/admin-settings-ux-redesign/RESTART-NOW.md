# 🔄 RESTART DEV SERVER NOW

## ⚠️ คุณต้องทำตอนนี้

```bash
# 1. หยุด dev server (กด Ctrl+C ในหน้าต่าง terminal)
^C

# 2. ลบ cache (แนะนำ)
rm -rf node_modules/.vite

# 3. เริ่ม dev server ใหม่
npm run dev
```

## ✅ หลังจาก restart แล้ว

1. เปิด: `http://localhost:5173/admin/settings`
2. คลิกการ์ด "ทั่วไป"
3. ตรวจสอบว่าไม่มี error 500

## 🎯 ทำไมต้อง restart?

เราได้แก้ไข:

- ✅ Tailwind 4 compatibility (ลบ `@apply` ออกทั้งหมด)
- ✅ Component exports (เปลี่ยนเป็น direct imports)
- ✅ URL display (เพิ่ม URL ในแต่ละการ์ด)
- ✅ Routing structure (แก้ไข router)

Vite ต้องการ restart เพื่อ:

- 🔄 Clear module cache
- 🔄 Re-parse Vue SFC files
- 🔄 Rebuild dependency graph

## 📋 ถ้ายังมี error หลัง restart

1. **Clear browser cache**: `Ctrl+Shift+R` (Windows) หรือ `Cmd+Shift+R` (Mac)
2. **ตรวจสอบ console**: ดูว่ามี error อะไรอีกไหม
3. **ลอง restart อีกครั้ง**: บางครั้ง Vite ต้องการ restart 2 ครั้ง

---

**พร้อมแล้ว? Restart เลย!** 🚀
