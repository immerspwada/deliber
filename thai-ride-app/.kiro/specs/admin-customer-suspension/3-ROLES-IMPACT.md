# ผลกระทบต่อ 3 Roles - Customer Suspension System

## ภาพรวม

ระบบระงับการใช้งานลูกค้าออกแบบมาเพื่อทำงานตามกฎ 3 roles: **Customer**, **Provider**, **Admin**

## 🎭 Role-Based Access Control

### 1. 👤 Customer (ลูกค้า)

#### สิทธิ์:

- ❌ **ไม่สามารถ** ระงับบัญชีใครได้
- ❌ **ไม่สามารถ** ปลดระงับบัญชีใครได้
- ❌ **ไม่สามารถ** ดูรายการลูกค้าที่ถูกระงับ

#### ผลกระทบเมื่อถูกระงับ:

**ทันทีที่ถูกระงับ:**

- ❌ ไม่สามารถ login เข้าแอปได้
- ❌ ไม่สามารถเข้าถึงข้อมูลตัวเองได้ (RLS blocked)
- ❌ ไม่สามารถสร้างออเดอร์ใหม่ได้
- ❌ ไม่สามารถอัปเดตโปรไฟล์ได้
- ❌ ไม่สามารถใช้ wallet ได้

**ยังทำได้:**

- ✅ ออเดอร์ที่กำลังดำเนินการยังทำงานต่อได้ (ไม่ยกเลิก)
- ✅ ข้อมูลยังอยู่ในระบบ (ไม่ถูกลบ)

**เมื่อปลดระงับ:**

- ✅ กลับมาใช้งานได้ทันที
- ✅ ข้อมูลทั้งหมดยังอยู่ (wallet, orders, saved places)

#### RLS Policies:

```sql
-- Customer ที่ถูกระงับไม่สามารถ SELECT ข้อมูลตัวเองได้
CREATE POLICY "customer_suspended_blocked" ON profiles
  FOR SELECT TO authenticated
  USING (
    CASE
      WHEN auth.uid() = id AND role = 'customer'
      THEN status != 'suspended'
      ELSE true
    END
  );

-- Customer ที่ถูกระงับไม่สามารถ UPDATE ข้อมูลได้
CREATE POLICY "customer_suspended_no_update" ON profiles
  FOR UPDATE TO authenticated
  USING (
    CASE
      WHEN auth.uid() = id AND role = 'customer'
      THEN status != 'suspended'
      ELSE true
    END
  );
```

---

### 2. 🚗 Provider (ผู้ให้บริการ)

#### สิทธิ์:

- ❌ **ไม่สามารถ** ระงับบัญชีใครได้
- ❌ **ไม่สามารถ** ปลดระงับบัญชีใครได้
- ❌ **ไม่สามารถ** ดูรายการลูกค้าที่ถูกระงับ

#### ผลกระทบ:

- ✅ **ไม่ได้รับผลกระทบ** จากระบบนี้
- ✅ Provider มีระบบจัดการแยกใน `providers_v2` table
- ✅ Provider ใช้ `status` ใน `providers_v2` (approved/pending/rejected)
- ✅ ไม่สามารถถูกระงับผ่านระบบนี้ได้

#### การป้องกัน:

```sql
-- ป้องกันการระงับ Provider
IF v_customer_role = 'provider' THEN
  RAISE EXCEPTION 'Cannot suspend provider accounts. Use provider management instead.';
END IF;
```

**เหตุผล:**

- Provider มีระบบจัดการแยก (approve/reject)
- Provider ต้องผ่านการตรวจสอบเอกสาร
- Provider มีผลกระทบต่อธุรกิจมากกว่า customer

---

### 3. 👑 Admin (ผู้ดูแลระบบ)

#### สิทธิ์:

- ✅ **สามารถ** ระงับบัญชี customer ได้
- ✅ **สามารถ** ปลดระงับบัญชี customer ได้
- ✅ **สามารถ** ดูรายการลูกค้าทั้งหมด
- ✅ **สามารถ** ดูเหตุผลการระงับ
- ✅ **สามารถ** ดูประวัติการระงับ

#### การตรวจสอบสิทธิ์:

```sql
-- ตรวจสอบ role ใน RPC function
SELECT role INTO v_admin_role
FROM profiles
WHERE id = auth.uid();

IF v_admin_role IS NULL OR v_admin_role != 'admin' THEN
  RAISE EXCEPTION 'Only admins can suspend customers';
END IF;
```

#### การป้องกัน:

```sql
-- ป้องกันการระงับ Admin
IF v_customer_role = 'admin' THEN
  RAISE EXCEPTION 'Cannot suspend admin accounts';
END IF;

-- ป้องกันการระงับ Provider
IF v_customer_role = 'provider' THEN
  RAISE EXCEPTION 'Cannot suspend provider accounts. Use provider management instead.';
END IF;

-- อนุญาตเฉพาะ Customer
IF v_customer_role != 'customer' THEN
  RAISE EXCEPTION 'Can only suspend customer accounts';
END IF;
```

#### UI Access:

- ✅ เข้าถึงได้ที่ `/admin/customers`
- ✅ ปุ่มระงับ/ปลดระงับ
- ✅ Modal ระบุเหตุผล
- ✅ ดูประวัติการระงับ

---

## 📊 Role Comparison Table

| Feature                        | Customer | Provider | Admin               |
| ------------------------------ | -------- | -------- | ------------------- |
| **ระงับบัญชี**                 | ❌       | ❌       | ✅ (เฉพาะ customer) |
| **ปลดระงับบัญชี**              | ❌       | ❌       | ✅ (เฉพาะ customer) |
| **ดูรายการที่ถูกระงับ**        | ❌       | ❌       | ✅                  |
| **ถูกระงับได้**                | ✅       | ❌       | ❌                  |
| **เข้าถึงข้อมูลเมื่อถูกระงับ** | ❌       | N/A      | N/A                 |
| **สร้างออเดอร์เมื่อถูกระงับ**  | ❌       | N/A      | N/A                 |

---

## 🔒 Security by Role

### Customer Security

- ✅ RLS policy บล็อกการเข้าถึงเมื่อถูกระงับ
- ✅ ไม่สามารถ bypass ด้วย API
- ✅ Session ยังใช้งานได้แต่ไม่สามารถเข้าถึงข้อมูล

### Provider Security

- ✅ ไม่สามารถถูกระงับผ่านระบบนี้
- ✅ มีระบบจัดการแยกใน providers_v2
- ✅ ป้องกันการระงับโดยไม่ตั้งใจ

### Admin Security

- ✅ ไม่สามารถถูกระงับ
- ✅ ป้องกันการระงับ admin อื่น
- ✅ Audit trail บันทึกทุกการกระทำ

---

## 🧪 Testing by Role

### Test as Customer

```sql
-- Login as customer
-- Try to suspend another customer
SELECT suspend_customer_account(
  'another-customer-uuid'::UUID,
  'Test reason'
);
-- Expected: ERROR: Only admins can suspend customers
```

### Test as Provider

```sql
-- Login as provider
-- Try to suspend a customer
SELECT suspend_customer_account(
  'customer-uuid'::UUID,
  'Test reason'
);
-- Expected: ERROR: Only admins can suspend customers
```

### Test as Admin

```sql
-- Login as admin
-- Suspend a customer (should succeed)
SELECT suspend_customer_account(
  'customer-uuid'::UUID,
  'Violation of terms'
);
-- Expected: {"success": true, ...}

-- Try to suspend a provider (should fail)
SELECT suspend_customer_account(
  'provider-uuid'::UUID,
  'Test reason'
);
-- Expected: ERROR: Cannot suspend provider accounts

-- Try to suspend another admin (should fail)
SELECT suspend_customer_account(
  'admin-uuid'::UUID,
  'Test reason'
);
-- Expected: ERROR: Cannot suspend admin accounts
```

---

## 📋 Production Checklist by Role

### Customer

- [ ] ลูกค้าที่ถูกระงับไม่สามารถ login ได้
- [ ] ลูกค้าที่ถูกระงับไม่สามารถสร้างออเดอร์ได้
- [ ] ลูกค้าที่ปลดระงับกลับมาใช้งานได้ทันที
- [ ] ข้อมูลลูกค้าไม่ถูกลบเมื่อระงับ

### Provider

- [ ] Provider ไม่สามารถถูกระงับผ่านระบบนี้
- [ ] Provider ยังทำงานปกติ
- [ ] Provider ไม่สามารถระงับ customer ได้

### Admin

- [ ] Admin สามารถระงับ customer ได้
- [ ] Admin สามารถปลดระงับ customer ได้
- [ ] Admin ไม่สามารถระงับ provider ได้
- [ ] Admin ไม่สามารถระงับ admin อื่นได้
- [ ] Admin เห็นรายการลูกค้าที่ถูกระงับ
- [ ] Audit trail บันทึกครบถ้วน

---

## 🚨 Common Issues by Role

### Customer Issues

**Issue:** ลูกค้าถูกระงับแต่ยัง login ได้

**Solution:**

```sql
-- Check RLS policy
SELECT * FROM pg_policies
WHERE tablename = 'profiles'
AND policyname = 'customer_suspended_blocked';

-- Check customer status
SELECT id, role, status FROM profiles WHERE id = 'customer-uuid';
```

### Provider Issues

**Issue:** พยายามระงับ provider แต่ error

**Solution:** นี่คือพฤติกรรมที่ถูกต้อง - Provider ไม่สามารถถูกระงับผ่านระบบนี้

### Admin Issues

**Issue:** Admin ไม่สามารถระงับได้

**Solution:**

```sql
-- Check admin role
SELECT id, email, role FROM profiles WHERE email = 'admin@example.com';

-- Update role if needed
UPDATE profiles SET role = 'admin' WHERE email = 'admin@example.com';
```

---

## 📖 Summary

ระบบระงับการใช้งานลูกค้าออกแบบมาเพื่อ:

1. **Customer**: ป้องกันการใช้งานที่ไม่เหมาะสม
2. **Provider**: ไม่ได้รับผลกระทบ (มีระบบแยก)
3. **Admin**: ควบคุมและจัดการระบบ

**หลักการสำคัญ:**

- ✅ เฉพาะ Admin ระงับได้
- ✅ ระงับได้เฉพาะ Customer
- ✅ Provider และ Admin ไม่สามารถถูกระงับ
- ✅ RLS policy ป้องกันการเข้าถึง
- ✅ Audit trail บันทึกทุกการกระทำ
