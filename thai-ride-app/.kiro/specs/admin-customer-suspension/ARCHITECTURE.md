# 🏗️ System Architecture

## Overview

```
┌─────────────────────────────────────────────────────────────┐
│                     Admin Panel UI                          │
│                 (CustomersViewEnhanced)                     │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ├─── Search & Filter
                     ├─── Select Multiple
                     ├─── View Details
                     └─── Suspend/Unsuspend
                     │
┌────────────────────▼────────────────────────────────────────┐
│                    Components Layer                          │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────────────────┐  ┌──────────────────────┐        │
│  │ CustomerSuspension   │  │  CustomerDetail      │        │
│  │      Modal           │  │      Modal           │        │
│  └──────────────────────┘  └──────────────────────┘        │
└────────────────────┬────────────────────────────────────────┘
                     │
┌────────────────────▼────────────────────────────────────────┐
│                 Composables Layer                            │
├─────────────────────────────────────────────────────────────┤
│              useCustomerSuspension()                         │
│  ┌─────────────────────────────────────────────────┐        │
│  │  • suspendCustomer(id, reason)                  │        │
│  │  • unsuspendCustomer(id)                        │        │
│  │  • bulkSuspendCustomers(ids[], reason)          │        │
│  │  • loading, error states                        │        │
│  └─────────────────────────────────────────────────┘        │
└────────────────────┬────────────────────────────────────────┘
                     │
┌────────────────────▼────────────────────────────────────────┐
│                  Supabase Client                             │
│                    (RPC Calls)                               │
└────────────────────┬────────────────────────────────────────┘
                     │
┌────────────────────▼────────────────────────────────────────┐
│                 Supabase Backend                             │
├─────────────────────────────────────────────────────────────┤
│  PostgreSQL Database + RLS + Realtime                       │
│                                                              │
│  ┌──────────────────────────────────────────────┐          │
│  │  RPC Functions (SECURITY DEFINER)            │          │
│  │  ┌────────────────────────────────────────┐  │          │
│  │  │ admin_suspend_customer()               │  │          │
│  │  │ admin_unsuspend_customer()             │  │          │
│  │  │ admin_bulk_suspend_customers()         │  │          │
│  │  │ admin_get_customers()                  │  │          │
│  │  └────────────────────────────────────────┘  │          │
│  └──────────────────────────────────────────────┘          │
│                                                              │
│  ┌──────────────────────────────────────────────┐          │
│  │  profiles Table                              │          │
│  │  ┌────────────────────────────────────────┐  │          │
│  │  │ id (PK)                                │  │          │
│  │  │ email                                  │  │          │
│  │  │ full_name                              │  │          │
│  │  │ phone_number                           │  │          │
│  │  │ role                                   │  │          │
│  │  │ status ← NEW                           │  │          │
│  │  │ suspended_at ← NEW                     │  │          │
│  │  │ suspension_reason ← NEW                │  │          │
│  │  │ created_at                             │  │          │
│  │  │ updated_at                             │  │          │
│  │  └────────────────────────────────────────┘  │          │
│  └──────────────────────────────────────────────┘          │
│                                                              │
│  ┌──────────────────────────────────────────────┐          │
│  │  RLS Policies                                │          │
│  │  • Only admins can call RPC functions        │          │
│  │  • Role check in function body               │          │
│  └──────────────────────────────────────────────┘          │
│                                                              │
│  ┌──────────────────────────────────────────────┐          │
│  │  Realtime Subscription                       │          │
│  │  • Listen to profiles table changes          │          │
│  │  • Filter: role = 'customer'                 │          │
│  │  • Auto-refresh UI on changes                │          │
│  └──────────────────────────────────────────────┘          │
└─────────────────────────────────────────────────────────────┘
```

## Data Flow

### 1. Suspend Customer Flow

```
User Action (Click Suspend)
    │
    ▼
Open CustomerSuspensionModal
    │
    ├─ Display form
    ├─ Require reason input
    └─ Validate input
    │
    ▼
User Confirms
    │
    ▼
useCustomerSuspension.suspendCustomer(id, reason)
    │
    ▼
Supabase RPC Call
    │
    ├─ supabase.rpc('admin_suspend_customer', {
    │     p_customer_id: id,
    │     p_reason: reason
    │   })
    │
    ▼
Backend Function Execution
    │
    ├─ Check if user is admin
    ├─ Validate inputs
    ├─ Update profiles table:
    │   • status = 'suspended'
    │   • suspended_at = NOW()
    │   • suspension_reason = reason
    │   • updated_at = NOW()
    │
    ▼
Database Update
    │
    ├─ Trigger Realtime event
    │
    ▼
Realtime Broadcast
    │
    ├─ All subscribed clients receive update
    │
    ▼
UI Auto-Refresh
    │
    ├─ loadCustomers() called
    ├─ Table updates
    ├─ Status badge changes
    └─ Toast notification
    │
    ▼
Success!
```

### 2. Real-time Update Flow

```
Database Change (Any Client)
    │
    ▼
PostgreSQL Trigger
    │
    ▼
Supabase Realtime
    │
    ├─ Detect change in profiles table
    ├─ Filter: role = 'customer'
    │
    ▼
Broadcast to Subscribers
    │
    ├─ All connected admin panels
    │
    ▼
Client Receives Event
    │
    ├─ realtimeChannel.on('postgres_changes', ...)
    │
    ▼
Auto-Refresh UI
    │
    ├─ loadCustomers()
    ├─ Update table
    └─ No user action needed!
    │
    ▼
UI Updated in Real-time!
```

## Component Hierarchy

```
CustomersViewEnhanced
├── Header
│   ├── Title
│   └── Stats (Active/Suspended counts)
│
├── Filters Bar
│   ├── Search Input (debounced)
│   ├── Status Filter Dropdown
│   └── Bulk Actions Button
│
├── Data Table
│   ├── Table Header
│   │   ├── Select All Checkbox
│   │   └── Column Headers
│   │
│   └── Table Body
│       └── Customer Rows
│           ├── Checkbox
│           ├── Customer Info
│           ├── Status Badge
│           └── Action Buttons
│               ├── View Details
│               ├── Suspend (if active)
│               └── Unsuspend (if suspended)
│
├── Pagination
│   ├── Page Info
│   └── Navigation Buttons
│
├── CustomerSuspensionModal (conditional)
│   ├── Header
│   ├── Form
│   │   └── Reason Textarea (if suspending)
│   ├── Error Display
│   └── Action Buttons
│
└── CustomerDetailModal (conditional)
    ├── Header
    ├── Basic Info Section
    ├── Suspension Info Section (if suspended)
    └── Action Buttons
```

## State Management

```typescript
// Component State
const customers = ref<Customer[]>([]); // Customer list
const totalCount = ref(0); // Total count
const loading = ref(false); // Loading state
const error = ref<string | null>(null); // Error state

// Filter State
const searchQuery = ref(""); // Search term
const statusFilter = ref(""); // Status filter
const currentPage = ref(1); // Current page
const pageSize = ref(20); // Items per page

// Selection State
const selectedCustomers = ref<string[]>([]); // Selected IDs

// Modal State
const showSuspensionModal = ref(false); // Modal visibility
const suspensionCustomerIds = ref<string[]>([]); // Target IDs
const isSuspending = ref(true); // Suspend vs Unsuspend
const showDetailModal = ref(false); // Detail modal
const selectedCustomer = ref<Customer | null>(null); // Selected customer

// Realtime State
let realtimeChannel: RealtimeChannel | null = null; // Subscription
```

## Security Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                      Security Layers                         │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Layer 1: Frontend Route Guard                              │
│  ┌────────────────────────────────────────────────┐        │
│  │  • Check if user is authenticated              │        │
│  │  • Check if user role is 'admin'               │        │
│  │  • Redirect if unauthorized                    │        │
│  └────────────────────────────────────────────────┘        │
│                                                              │
│  Layer 2: RPC Function Authorization                        │
│  ┌────────────────────────────────────────────────┐        │
│  │  IF NOT EXISTS (                                │        │
│  │    SELECT 1 FROM profiles                       │        │
│  │    WHERE id = auth.uid()                        │        │
│  │    AND role = 'admin'                           │        │
│  │  ) THEN                                         │        │
│  │    RAISE EXCEPTION 'Unauthorized'               │        │
│  │  END IF;                                        │        │
│  └────────────────────────────────────────────────┘        │
│                                                              │
│  Layer 3: RLS Policies                                      │
│  ┌────────────────────────────────────────────────┐        │
│  │  • Row-level security enabled                   │        │
│  │  • Policies check user role                     │        │
│  │  • SECURITY DEFINER functions                   │        │
│  └────────────────────────────────────────────────┘        │
│                                                              │
│  Layer 4: Input Validation                                  │
│  ┌────────────────────────────────────────────────┐        │
│  │  • Reason required for suspension               │        │
│  │  • Trim whitespace                              │        │
│  │  • SQL injection protection (parameterized)     │        │
│  └────────────────────────────────────────────────┘        │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## Performance Optimizations

```
┌─────────────────────────────────────────────────────────────┐
│                   Performance Strategy                       │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Database Level                                              │
│  ┌────────────────────────────────────────────────┐        │
│  │  • Indexes on frequently queried columns       │        │
│  │    - idx_profiles_status                       │        │
│  │    - idx_profiles_email                        │        │
│  │    - idx_profiles_phone                        │        │
│  │  • Pagination (LIMIT/OFFSET)                   │        │
│  │  • Selective column fetching                   │        │
│  └────────────────────────────────────────────────┘        │
│                                                              │
│  Application Level                                           │
│  ┌────────────────────────────────────────────────┐        │
│  │  • Debounced search (300ms)                    │        │
│  │  • Lazy loading components                     │        │
│  │  • Efficient re-renders (v-memo)               │        │
│  │  • Computed properties for derived data        │        │
│  └────────────────────────────────────────────────┘        │
│                                                              │
│  Network Level                                               │
│  ┌────────────────────────────────────────────────┐        │
│  │  • Real-time subscription (no polling)         │        │
│  │  • Single RPC call for bulk operations         │        │
│  │  • Optimistic UI updates                       │        │
│  └────────────────────────────────────────────────┘        │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## Error Handling Flow

```
Error Occurs
    │
    ▼
Catch in Composable
    │
    ├─ Set error.value
    ├─ Log to console
    └─ Throw error
    │
    ▼
Catch in Component
    │
    ├─ Display error in modal
    ├─ Show toast notification
    └─ Enable retry
    │
    ▼
User Can:
    ├─ Retry action
    ├─ Close modal
    └─ View error details
```

## Deployment Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    Production Setup                          │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Frontend (Vercel)                                           │
│  ┌────────────────────────────────────────────────┐        │
│  │  • Static assets (CDN)                         │        │
│  │  • Vue 3 SPA                                   │        │
│  │  • Auto-scaling                                │        │
│  └────────────────────────────────────────────────┘        │
│                     │                                        │
│                     ▼                                        │
│  Backend (Supabase)                                          │
│  ┌────────────────────────────────────────────────┐        │
│  │  • PostgreSQL (managed)                        │        │
│  │  • Realtime (WebSocket)                        │        │
│  │  • Auth (JWT)                                  │        │
│  │  • Storage (S3-compatible)                     │        │
│  └────────────────────────────────────────────────┘        │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

**Architecture Version**: 1.0.0  
**Last Updated**: 2026-01-18  
**Status**: 🟢 Stable
