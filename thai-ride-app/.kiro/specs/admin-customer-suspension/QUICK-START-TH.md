# 🚀 คู่มือเริ่มต้นใช้งาน - ระบบระงับผู้ใช้งาน

## เริ่มต้นใช้งาน

### 1. Apply Migration

```bash
# เริ่ม Supabase
npx supabase start

# Migration จะ apply อัตโนมัติ
# หรือ apply ด้วยตัวเอง:
npx supabase db push --local
```

### 2. ใช้งานใน Admin Panel

เปิดไฟล์ `src/admin/router.ts` และเพิ่ม route:

```typescript
{
  path: '/admin/customers',
  component: () => import('@/admin/views/CustomersViewEnhanced.vue'),
  meta: {
    requiresAuth: true,
    allowedRoles: ['admin'],
  },
}
```

### 3. เข้าใช้งาน

เปิดเบราว์เซอร์ไปที่:

```
http://localhost:5173/admin/customers
```

## การใช้งาน

### ระงับผู้ใช้งานเดี่ยว

1. คลิกปุ่ม 🚫 ที่แถวของลูกค้า
2. ระบุเหตุผล (จำเป็น)
3. คลิก "ระงับ"

### ระงับหลายคน

1. เลือก checkbox ที่ต้องการ
2. คลิกปุ่ม "ระงับที่เลือก (X)"
3. ระบุเหตุผล
4. คลิก "ระงับ"

### ยกเลิกการระงับ

1. คลิกปุ่ม ✅ ที่แถวของลูกค้าที่ถูกระงับ
2. ยืนยันการยกเลิก

### ค้นหา

พิมพ์ในช่องค้นหา:

- ชื่อลูกค้า
- อีเมล
- เบอร์โทร

ระบบจะค้นหาอัตโนมัติหลัง 300ms

### กรองตามสถานะ

เลือกจาก dropdown:

- สถานะทั้งหมด
- ใช้งานปกติ
- ถูกระงับ
- ถูกแบน

## Real-time Updates

ระบบจะอัปเดตอัตโนมัติเมื่อ:

- มีการระงับ/ยกเลิกการระงับ
- มีลูกค้าใหม่
- มีการเปลี่ยนแปลงข้อมูล

**ไม่ต้อง refresh หน้าเว็บ!** 🎉

## ฟีเจอร์

### ✅ ระงับผู้ใช้งาน

- ระงับเดี่ยว
- ระงับหลายคน
- ต้องระบุเหตุผล

### ✅ ยกเลิกการระงับ

- ยกเลิกเดี่ยว
- ยกเลิกหลายคน

### ✅ ค้นหาและกรอง

- ค้นหาแบบ real-time
- กรองตามสถานะ
- Pagination

### ✅ ดูรายละเอียด

- ข้อมูลพื้นฐาน
- ประวัติการระงับ
- วันที่และเหตุผล

## Keyboard Shortcuts

| Key     | Action                  |
| ------- | ----------------------- |
| `/`     | Focus ช่องค้นหา         |
| `Esc`   | ปิด Modal               |
| `Enter` | ยืนยันการระงับ          |
| `Tab`   | Navigate ระหว่าง fields |

## สถานะผู้ใช้งาน

| สถานะ         | ความหมาย                   | สี    |
| ------------- | -------------------------- | ----- |
| 🟢 ใช้งานปกติ | ใช้งานได้ปกติ              | เขียว |
| 🔴 ถูกระงับ   | ไม่สามารถใช้งานได้ชั่วคราว | แดง   |
| ⚫ ถูกแบน     | ไม่สามารถใช้งานได้ถาวร     | เทา   |

## Error Messages

| Message              | สาเหตุ          | แก้ไข                    |
| -------------------- | --------------- | ------------------------ |
| "กรุณาระบุเหตุผล"    | ไม่ได้ใส่เหตุผล | ใส่เหตุผลในช่อง          |
| "Unauthorized"       | ไม่ใช่ Admin    | Login ด้วย Admin account |
| "Customer not found" | ไม่พบลูกค้า     | Refresh หน้าเว็บ         |

## Performance Tips

### ⚡ เร็วขึ้น

- ใช้ pagination แทนการโหลดทั้งหมด
- ใช้ filter เพื่อลดจำนวนข้อมูล
- ค้นหาเฉพาะที่ต้องการ

### 🎯 ประหยัด

- Real-time subscription ใช้ bandwidth น้อย
- Debounced search ลด API calls
- Indexed queries รวดเร็ว

## Troubleshooting

### ❌ Real-time ไม่ทำงาน

```typescript
// เช็ค subscription status
console.log(realtimeChannel.state);
// ควรเป็น 'joined'
```

**แก้ไข:**

1. Refresh หน้าเว็บ
2. ตรวจสอบ Supabase connection
3. ดู Console logs

### ❌ ระงับไม่ได้

**เช็คว่า:**

1. ✅ Login ด้วย Admin account
2. ✅ ใส่เหตุผลแล้ว
3. ✅ Supabase กำลังทำงาน

### ❌ ช้า

**แก้ไข:**

1. ใช้ filter ลดจำนวนข้อมูล
2. เพิ่ม indexes (ทำแล้วใน migration)
3. ตรวจสอบ network connection

## API Reference

### RPC Functions

```typescript
// ระงับเดี่ยว
await supabase.rpc("admin_suspend_customer", {
  p_customer_id: "uuid",
  p_reason: "string",
});

// ยกเลิกการระงับ
await supabase.rpc("admin_unsuspend_customer", {
  p_customer_id: "uuid",
});

// ระงับหลายคน
await supabase.rpc("admin_bulk_suspend_customers", {
  p_customer_ids: ["uuid1", "uuid2"],
  p_reason: "string",
});

// ดึงข้อมูลลูกค้า
await supabase.rpc("admin_get_customers", {
  p_search: "string",
  p_status: ["active", "suspended"],
  p_limit: 20,
  p_offset: 0,
});
```

### Composable

```typescript
import { useCustomerSuspension } from "@/admin/composables/useCustomerSuspension";

const {
  loading, // ref<boolean>
  error, // ref<string | null>
  suspendCustomer, // (id, reason) => Promise
  unsuspendCustomer, // (id) => Promise
  bulkSuspendCustomers, // (ids[], reason) => Promise
} = useCustomerSuspension();
```

## Best Practices

### ✅ ควรทำ

- ระบุเหตุผลที่ชัดเจน
- ตรวจสอบข้อมูลก่อนระงับ
- ใช้ bulk action สำหรับหลายคน
- ดูรายละเอียดก่อนตัดสินใจ

### ❌ ไม่ควรทำ

- ระงับโดยไม่มีเหตุผล
- ระงับผู้ใช้งานผิดคน
- ใช้เหตุผลที่ไม่เหมาะสม
- ลืมยกเลิกการระงับ

## Security

### 🔒 ความปลอดภัย

- ✅ เฉพาะ Admin เท่านั้น
- ✅ RLS policies ป้องกัน
- ✅ Audit log (coming soon)
- ✅ Input validation

### 🛡️ Privacy

- ข้อมูลส่วนตัวถูกปกป้อง
- เฉพาะ Admin เห็นได้
- ไม่มี public API

## Support

### 📚 เอกสารเพิ่มเติม

- [IMPLEMENTATION-COMPLETE.md](./IMPLEMENTATION-COMPLETE.md)
- [Database Schema](../../migrations/312_customer_suspension_system.sql)
- [Tests](../../tests/admin-customer-suspension-realtime.unit.test.ts)

### 🐛 พบปัญหา?

1. เช็ค Console logs
2. ดู Error messages
3. ลอง Refresh
4. ติดต่อ Dev team

## Updates

### Version 1.0.0 (2026-01-18)

- ✅ Initial release
- ✅ Real-time updates
- ✅ Bulk actions
- ✅ Mobile support

---

**Happy Suspending! 🎉**

_ระบบนี้ออกแบบมาเพื่อความสะดวกและปลอดภัย_
