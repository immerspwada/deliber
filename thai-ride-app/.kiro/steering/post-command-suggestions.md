---
inclusion: always
---

# 📋 Post-Command Suggestions (คำแนะนำหลังจบคำสั่ง)

หลังจากทำงานเสร็จทุกครั้ง ให้แนะนำ **3 ฟีเจอร์ใหม่** และ **3 สิ่งที่ควรทำ** โดยต้องสอดคล้องกับคำสั่งล่าสุดที่ทำ

## 🎯 หลักการแนะนำ

1. **ต้องเกี่ยวข้องกับงานที่เพิ่งทำ** - ไม่ใช่แนะนำแบบทั่วไป
2. **เรียงลำดับความสำคัญ** - สิ่งที่ควรทำก่อนอยู่บน
3. **ระบุเหตุผล** - ทำไมถึงควรทำ
4. **ให้คำสั่งที่พร้อมใช้** - ถ้าเป็นไปได้

## 📝 รูปแบบการแนะนำ

```markdown
## 💡 ฟีเจอร์ใหม่ที่แนะนำ:

1. **[ชื่อฟีเจอร์]** - [คำอธิบายสั้นๆ]
2. **[ชื่อฟีเจอร์]** - [คำอธิบายสั้นๆ]
3. **[ชื่อฟีเจอร์]** - [คำอธิบายสั้นๆ]

## ✅ สิ่งที่ควรทำต่อ:

1. **[หัวข้อ]** - [เหตุผล]
2. **[หัวข้อ]** - [เหตุผล]
3. **[หัวข้อ]** - [เหตุผล]
```

## 🔄 ตัวอย่างตามประเภทงาน

### หลังสร้าง/แก้ไข Component

**💡 ฟีเจอร์ใหม่:**

1. **Animation/Transition** - เพิ่ม micro-interactions
2. **Loading Skeleton** - แสดง placeholder ขณะโหลด
3. **Error State UI** - แสดงข้อความเมื่อเกิดข้อผิดพลาด

**✅ สิ่งที่ควรทำ:**

1. **ทดสอบบน Mobile** - เพราะเป็น Mobile-First
2. **ตรวจสอบ Accessibility** - ทุก component ต้อง accessible
3. **เพิ่ม Unit Test** - เพิ่ม test coverage

### หลังแก้ไข Database/Migration

**💡 ฟีเจอร์ใหม่:**

1. **Audit Logging** - บันทึกการเปลี่ยนแปลงข้อมูล
2. **Soft Delete** - ลบแบบ recoverable
3. **Data Archiving** - ย้ายข้อมูลเก่าไป archive

**✅ สิ่งที่ควรทำ:**

1. **รัน Migration** - `supabase db push`
2. **ตรวจสอบ RLS Policies** - ความปลอดภัย
3. **อัพเดท TypeScript Types** - sync กับ database

### หลังแก้ไข API/Edge Function

**💡 ฟีเจอร์ใหม่:**

1. **Caching Layer** - เพิ่ม response caching
2. **Webhook Support** - แจ้งเตือนระบบภายนอก
3. **Batch Operations** - รองรับ bulk requests

**✅ สิ่งที่ควรทำ:**

1. **ทดสอบ Endpoint** - ตรวจสอบ response
2. **ตรวจสอบ Error Handling** - จัดการ errors ครบ
3. **เพิ่ม Rate Limiting** - ป้องกัน abuse

### หลังแก้ไข Styling/UI

**💡 ฟีเจอร์ใหม่:**

1. **Dark Mode** - รองรับธีมมืด
2. **Haptic Feedback** - สั่นเมื่อกดปุ่ม (mobile)
3. **Pull to Refresh** - ดึงลงเพื่อรีเฟรช

**✅ สิ่งที่ควรทำ:**

1. **ทดสอบ Responsive** - ทุก breakpoint
2. **ทดสอบ Touch Targets** - ≥ 44px
3. **ตรวจสอบ Color Contrast** - ≥ 4.5:1

### หลังแก้ไข State/Store

**💡 ฟีเจอร์ใหม่:**

1. **Offline Persistence** - เก็บ state เมื่อ offline
2. **Optimistic Updates** - อัพเดท UI ก่อน API response
3. **State History** - undo/redo support

**✅ สิ่งที่ควรทำ:**

1. **ทดสอบ State Changes** - ค่าถูกต้อง
2. **ตรวจสอบ Memory Leaks** - cleanup subscriptions
3. **ทดสอบ Edge Cases** - null, empty, error states

### หลังแก้ไข Composable

**💡 ฟีเจอร์ใหม่:**

1. **Retry Logic** - ลองใหม่อัตโนมัติเมื่อ fail
2. **Debounce/Throttle** - ควบคุม rate ของ operations
3. **Cancel Support** - ยกเลิก pending operations

**✅ สิ่งที่ควรทำ:**

1. **ตรวจสอบ Cleanup** - onUnmounted
2. **ทดสอบ Error States** - จัดการ errors
3. **เพิ่ม JSDoc** - documentation

### หลังแก้ไข Security

**💡 ฟีเจอร์ใหม่:**

1. **2FA Authentication** - ยืนยันตัวตน 2 ขั้นตอน
2. **Session Management** - จัดการ active sessions
3. **Security Alerts** - แจ้งเตือนกิจกรรมผิดปกติ

**✅ สิ่งที่ควรทำ:**

1. **ทดสอบ RLS Policies** - ทุก role
2. **ตรวจสอบ Secrets** - ไม่มี hardcode
3. **รัน Security Scan** - หา vulnerabilities

### หลังแก้ไข Performance

**💡 ฟีเจอร์ใหม่:**

1. **Service Worker Caching** - cache static assets
2. **Image Lazy Loading** - โหลดรูปเมื่อเห็น
3. **Virtual Scrolling** - สำหรับ list ยาวๆ

**✅ สิ่งที่ควรทำ:**

1. **วัด Before/After** - เปรียบเทียบ
2. **ตรวจสอบ Bundle Size** - ไม่เพิ่มมาก
3. **ทดสอบ Lighthouse** - score ≥ 90

## 🚀 คำสั่งที่ใช้บ่อย

```bash
# ตรวจสอบ TypeScript
npm run type-check

# รัน Tests
npm run test

# ตรวจสอบ Lint
npm run lint

# Build Production
npm run build

# รัน Migration
supabase db push
```

## 📌 ตัวอย่างการใช้งานจริง

### ตัวอย่าง: หลังเพิ่ม Nearby Places ใน Search Box

```markdown
## 💡 ฟีเจอร์ใหม่ที่แนะนำ:

1. **Search History** - บันทึกประวัติการค้นหาเพื่อแสดงเป็น suggestions
2. **Favorite Places** - ให้ผู้ใช้บันทึกสถานที่โปรดได้
3. **Voice Search** - ค้นหาด้วยเสียงพูด

## ✅ สิ่งที่ควรทำต่อ:

1. **ทดสอบการแสดงผล** - เปิด http://localhost:5173/customer/ride และคลิกช่องค้นหา
2. **ทดสอบบน Mobile** - เปิด DevTools → Toggle device toolbar
3. **ตรวจสอบ API Rate Limiting** - ดู Console ว่าไม่มี 429 errors
```

### ตัวอย่าง: หลังแก้ไข Database Migration

```markdown
## 💡 ฟีเจอร์ใหม่ที่แนะนำ:

1. **Data Export** - ให้ผู้ใช้ export ข้อมูลของตัวเองได้
2. **Backup Automation** - สร้าง scheduled backup
3. **Data Analytics Dashboard** - แสดงสถิติการใช้งาน

## ✅ สิ่งที่ควรทำต่อ:

1. **รัน Migration** - `supabase db push`
2. **อัพเดท Types** - `supabase gen types typescript --local > src/types/database.ts`
3. **ทดสอบ RLS Policies** - ทดสอบ query ด้วย user ต่างๆ
```

### ตัวอย่าง: หลังสร้าง Steering Rule ใหม่

```markdown
## 💡 ฟีเจอร์ใหม่ที่แนะนำ:

1. **Rule Templates** - สร้าง template สำหรับ steering rules ที่ใช้บ่อย
2. **Rule Validation** - ตรวจสอบ syntax ของ steering rules อัตโนมัติ
3. **Rule Analytics** - ติดตามว่า rules ไหนถูกใช้บ่อย

## ✅ สิ่งที่ควรทำต่อ:

1. **ทดสอบว่า Rule ถูกโหลด** - ลองถามคำถามใหม่
2. **ตรวจสอบ Front-matter** - ยืนยันว่ามี `inclusion: always`
3. **Commit Changes** - `git add .kiro/steering/ && git commit -m "feat: add new steering rule"`
```

---

**หมายเหตุ**: การแนะนำต้องปรับตามบริบทของงานที่ทำ ไม่ใช่ copy-paste แบบเดิมทุกครั้ง
