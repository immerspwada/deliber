# Thai Ride App - ระบบเรียกรถไทย

แอปพลิเคชัน Progressive Web App (PWA) สำหรับบริการเรียกรถ ส่งของ และซื้อของ ในประเทศไทย

## ฟีเจอร์หลัก

- 🚗 **เรียกรถ** - บริการรับส่งที่สะดวกและปลอดภัย
- 📦 **ส่งของ** - ส่งของรวดเร็วและเชื่อถือได้
- 🛒 **ซื้อของ** - มีคนไปซื้อของให้คุณ
- 📱 **PWA** - ใช้งานได้เหมือนแอปดั้งเดิม
- 🇹🇭 **ภาษาไทย** - รองรับภาษาไทยเต็มรูปแบบ
- 💳 **ชำระเงิน** - รองรับ PromptPay และระบบธนาคารไทย
- 💰 **ระบบกระเป๋าเงิน** - ระบบเติมเงินและจัดการยอดเงินแบบเรียลไทม์

## เทคโนโลยี

- **Frontend**: Vue.js 3 + TypeScript + Vite
- **UI**: Tailwind CSS (Uber-like design)
- **PWA**: Vite PWA Plugin + Workbox
- **State Management**: Pinia
- **Router**: Vue Router 4
- **Backend**: Supabase (PostgreSQL + Auth + Storage)
- **Maps**: Google Maps API
- **Testing**: Vitest + Vue Test Utils + Fast-check

## การติดตั้ง

1. Clone repository

```bash
git clone <repository-url>
cd thai-ride-app
```

2. ติดตั้ง dependencies

```bash
npm install
```

3. ตั้งค่า environment variables

```bash
cp .env.example .env
# แก้ไขค่าใน .env ตามที่ต้องการ
```

4. รันแอปพลิเคชัน

```bash
npm run dev
```

## Scripts

- `npm run dev` - รัน development server
- `npm run build` - build สำหรับ production
- `npm run preview` - preview build
- `npm run test` - รัน tests
- `npm run test:watch` - รัน tests แบบ watch mode
- `npm run test:ui` - รัน tests พร้อม UI

## โครงสร้างโปรเจค

```
thai-ride-app/
├── public/                 # Static assets
├── src/
│   ├── admin/             # Admin panel (Vue 3 + TypeScript)
│   │   ├── components/    # Admin-specific components
│   │   ├── views/         # Admin pages
│   │   ├── composables/   # Admin composables
│   │   └── router.ts      # Admin routes
│   ├── components/        # Shared Vue components
│   ├── views/             # Customer/Provider pages
│   ├── router/            # Vue Router configuration
│   ├── stores/            # Pinia stores
│   ├── tests/             # Test files
│   ├── types/             # TypeScript type definitions
│   └── utils/             # Utility functions
├── supabase/
│   ├── migrations/        # Database migrations
│   └── functions/         # Edge functions
├── docs/                  # Documentation
├── .env                   # Environment variables
└── vite.config.ts         # Vite configuration
```

## ระบบหลัก

### ระบบกระเป๋าเงิน (Wallet System)

- **เติมเงิน**: ลูกค้าสามารถเติมเงินผ่านช่องทางต่างๆ (โอนเงิน, PromptPay, Mobile Banking)
- **การอนุมัติ**: Admin ตรวจสอบและอนุมัติคำขอเติมเงิน
- **ประวัติ**: ติดตามประวัติการทำธุรกรรมทั้งหมด
- **ความปลอดภัย**: RLS policies ป้องกันการเข้าถึงข้อมูลโดยไม่ได้รับอนุญาต

### Admin Panel

- **จัดการลูกค้า**: ดูข้อมูล ค้นหา และจัดการบัญชีลูกค้า
- **จัดการผู้ให้บริการ**: อนุมัติ/ปฏิเสธผู้ให้บริการใหม่
- **คำขอเติมเงิน**: ตรวจสอบและอนุมัติคำขอเติมเงินจากลูกค้า (พร้อม UI ที่ปรับปรุงใหม่)
- **คำขอถอนเงิน**: จัดการคำขอถอนเงินของผู้ให้บริการ
- **รายงานและสถิติ**: ดูรายงานรายได้และการชำระเงิน
- **การตั้งค่า**: จัดการค่าคอมมิชชั่น ค่าธรรมเนียม และการตั้งค่าระบบ
  - **การตั้งค่าทางการเงิน**: จัดการอัตราคอมมิชชั่น การถอนเงิน และการเติมเงิน
  - **ตั้งค่าการเติมเงิน**: กำหนดจำนวนเงินขั้นต่ำ/สูงสุด ค่าธรรมเนียม และเงื่อนไขการเติมเงิน

**UI/UX Enhancements** (2026-01-22):

- ✨ Enhanced table design with gradient headers and icon-enhanced columns
- ✨ Improved visual hierarchy with SVG icons for better scanning
- ✨ Modern card-based layout with shadows and rounded corners
- ✨ Responsive design optimized for mobile and desktop

## การพัฒนา

### Database Migrations

โปรเจคใช้ Supabase สำหรับ backend และ database:

```bash
# ดู migration ล่าสุด
ls -la supabase/migrations/

# Migration ที่สำคัญ:
# - 316_topup_requests_system.sql - ระบบคำขอเติมเงิน
# - 317_fix_topup_function_conflict.sql - แก้ไขปัญหา function conflict (ครั้งที่ 1)
# - 318_verify_and_fix_topup_conflicts.sql - แก้ไขปัญหา function conflict แบบครอบคลุม (ครั้งที่ 2)
# - 297_admin_priority1_rpc_functions.sql - Admin RPC functions
# - 299_admin_priority3_rpc_functions.sql - Analytics functions
```

### Admin RPC Functions

ระบบมี RPC functions สำหรับ Admin Panel:

**Customer Management**:

- `get_admin_customers()` - ดึงรายการลูกค้า
- `count_admin_customers()` - นับจำนวนลูกค้า

**Provider Management**:

- `get_admin_providers_v2()` - ดึงรายการผู้ให้บริการ
- `count_admin_providers_v2()` - นับจำนวนผู้ให้บริการ

**Financial Management**:

- `get_topup_requests_admin()` - ดึงคำขอเติมเงิน
- `count_topup_requests_admin()` - นับจำนวนคำขอเติมเงิน
- `approve_topup_request()` - อนุมัติคำขอเติมเงิน
- `reject_topup_request()` - ปฏิเสธคำขอเติมเงิน
- `get_provider_withdrawals_admin()` - ดึงคำขอถอนเงิน
- `count_provider_withdrawals_admin()` - นับจำนวนคำขอถอนเงิน

**Analytics**:

- `get_admin_revenue_stats()` - สถิติรายได้
- `get_admin_payment_stats()` - สถิติการชำระเงิน

ดูเอกสารเพิ่มเติมที่ `docs/admin-rpc-functions.md`

### การเพิ่มฟีเจอร์ใหม่

1. สร้าง component ใน `src/components/` หรือ `src/admin/components/`
2. สร้าง view ใน `src/views/` หรือ `src/admin/views/`
3. เพิ่ม route ใน `src/router/index.ts` หรือ `src/admin/router.ts`
4. เขียน test ใน `src/tests/`
5. อัพเดท types ใน `src/types/`

### การทดสอบ

- **Unit Tests**: ใช้ Vitest + Vue Test Utils
- **Property-Based Tests**: ใช้ Fast-check
- **E2E Tests**: จะเพิ่มในอนาคต

### PWA Features

- ✅ Service Worker
- ✅ Web App Manifest
- ✅ Offline Support
- ✅ Install Prompt
- ✅ Push Notifications (พร้อมใช้งาน)

## การ Deploy

1. Build แอปพลิเคชัน

```bash
npm run build
```

2. Deploy ไฟล์ใน `dist/` ไปยัง hosting service
   - Vercel (แนะนำ)
   - Netlify
   - Firebase Hosting

## การมีส่วนร่วม

1. Fork repository
2. สร้าง feature branch
3. Commit การเปลี่ยนแปลง
4. Push ไปยัง branch
5. สร้าง Pull Request

## License

MIT License

## ติดต่อ

สำหรับคำถามหรือข้อเสนอแนะ กรุณาติดต่อผ่าน GitHub Issues
