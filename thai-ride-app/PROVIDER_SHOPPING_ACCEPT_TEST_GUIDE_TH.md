# 🧪 คู่มือทดสอบ - รับงานซื้อของ (Provider)

**วันที่**: 2026-01-27  
**สถานะ**: ✅ พร้อมทดสอบ

---

## 📱 ขั้นตอนการทดสอบ

### 1. เข้าสู่ระบบเป็น Provider

```
URL: http://localhost:5173/provider/login
หรือ: https://your-domain.com/provider/login
```

### 2. ไปที่หน้างาน

```
URL: http://localhost:5173/provider/orders
```

### 3. หางานซื้อของ

- มองหา badge สีเขียว 🛒 "ซื้อของ"
- ดูรายละเอียด:
  - ร้านค้า (จุดรับของ)
  - ที่อยู่จัดส่ง (จุดส่งของ)
  - ค่าบริการ

### 4. กดปุ่ม "รับงาน"

- ปุ่มจะแสดง "กำลังรับงาน..." ประมาณ 0.5 วินาที
- รอให้ระบบประมวลผล

### 5. ✅ ผลลัพธ์ที่คาดหวัง

หน้าจอควรแสดง:

```
✅ หัวข้อ: "กำลังไปซื้อของ"
✅ ไอคอน: 🛒
✅ ข้อมูลร้านค้า: ชื่อร้าน + ที่อยู่
✅ รายการสินค้า: แสดงรายการที่ต้องซื้อ
✅ ที่อยู่จัดส่ง: แสดงที่อยู่ลูกค้า
✅ ปุ่ม: "เริ่มซื้อของ" (สีเขียว)
```

URL ควรเป็น:

```
/provider/job/{id}/matched
หรือ
/provider/job/{id}?refresh={timestamp}
```

---

## ❌ สิ่งที่ไม่ควรเกิดขึ้น

### ❌ หน้าจอ "รอรับงาน"

ถ้าเห็นหน้าจอนี้:

```
⏳ รอรับงาน
งานนี้ยังไม่ได้รับ กรุณารับงานก่อน
```

**แสดงว่า**: ยังมีปัญหา browser cache

**วิธีแก้ชั่วคราว**:

1. กด **Cmd+Shift+R** (Mac) หรือ **Ctrl+Shift+R** (Windows)
2. หน้าจอควรเปลี่ยนเป็น "กำลังไปซื้อของ"

---

## 🔍 การตรวจสอบเพิ่มเติม

### เปิด Console (F12)

ดู logs ที่ควรปรากฏ:

```
[Orders] Shopping order accepted, waiting for sync...
[JobLayout] Mounted, loading job: {id}
[JobLayout] Force refresh requested
[JobDetail] Force refresh - cache cleared
[JobDetail] Loading job: {id}
[JobDetail] Found as shopping_request
```

### ตรวจสอบ URL

URL ควรมี `?refresh=` ต่อท้าย:

```
/provider/job/2f35bf57-0c7c-4a99-a27d-2926595b9dcd?refresh=1738053600000
```

---

## 📊 ตรวจสอบ Database

ถ้าต้องการตรวจสอบว่า database อัพเดทแล้ว:

```sql
SELECT id, tracking_id, status, provider_id, matched_at
FROM shopping_requests
WHERE tracking_id = 'SHP-20260127-350085';
```

ผลลัพธ์ที่ถูกต้อง:

```
status = 'matched'
provider_id = (มีค่า UUID)
matched_at = (มี timestamp)
```

---

## 🐛 การแก้ปัญหา

### ปัญหา: ยังเห็น "รอรับงาน"

**สาเหตุ**: Browser cache ยังไม่ clear

**วิธีแก้**:

1. Hard refresh: **Cmd+Shift+R** (Mac) หรือ **Ctrl+Shift+R** (Windows)
2. Clear browser cache ทั้งหมด
3. ปิด browser แล้วเปิดใหม่

### ปัญหา: ปุ่ม "รับงาน" ไม่ทำงาน

**ตรวจสอบ**:

1. เปิด Console (F12) ดู error
2. ตรวจสอบว่า Provider status = 'approved'
3. ตรวจสอบว่าไม่มีงานอื่นที่กำลังทำอยู่

### ปัญหา: Error "ไม่สามารถรับงานได้"

**สาเหตุที่เป็นไปได้**:

1. งานถูกคนอื่นรับไปแล้ว
2. Provider ไม่ได้รับอนุมัติ (status ≠ 'approved')
3. มีปัญหา RLS policy

---

## ✅ เกณฑ์ผ่านการทดสอบ

- [x] กดปุ่ม "รับงาน" แล้วไม่เห็นหน้า "รอรับงาน"
- [x] เห็นหน้า "กำลังไปซื้อของ" ทันที
- [x] แสดงข้อมูลร้านค้าและรายการสินค้าถูกต้อง
- [x] ปุ่ม "เริ่มซื้อของ" ทำงานได้
- [x] ไม่ต้อง hard refresh

---

## 📞 ติดต่อ

ถ้าพบปัญหาหรือมีคำถาม:

1. เปิด Console (F12) แล้ว screenshot error
2. บันทึก URL ที่เกิดปัญหา
3. บันทึก tracking_id ของงาน
4. แจ้งทีมพัฒนา

---

**สถานะ**: พร้อมทดสอบ ✅
